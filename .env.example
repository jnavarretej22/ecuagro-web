# Copia este archivo a `.env` y rellena los valores.
# Nunca subas `.env` al repositorio.

# Neon PostgreSQL (Connection string con sslmode=require)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"

# Groq (Fase 6+; clave solo en servidor, nunca en el cliente)
GROQ_API_KEY=
GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct

# Prueba manual POST /api/dev/groq-test (solo desarrollo; no activar en producción pública)
ENABLE_GROQ_TEST_ROUTE=

# Sesión: clave HS256 para firmar el JWT (mínimo 32 caracteres, sin espacios extra al pegar). Fase 3+
SESSION_SECRET=

# Usuario admin inicial (seed `npm run db:seed`; ver Fase 4 del plan)
BOOTSTRAP_ADMIN_USER=
BOOTSTRAP_ADMIN_PASSWORD=

# ── Proveedor de visión ──────────────────────────────────────────────────────
# VISION_PROVIDER controla qué motor de IA se usa para el diagnóstico.
# Valores: gemini | groq | openai
#   gemini  → activo (GEMINI_API_KEY + GEMINI_MODEL)
#   groq    → fallback cuando se agoten tokens de Gemini (GROQ_API_KEY + GROQ_MODEL)
#   openai  → stub; no hay integración real aún
# Si se deja vacío: usa Gemini si hay GEMINI_API_KEY, si no cae en Groq.
VISION_PROVIDER=gemini

# Gemini (proveedor principal)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite

# OpenAI / GPT‑4o (stub; implementar cuando se active)
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4o
